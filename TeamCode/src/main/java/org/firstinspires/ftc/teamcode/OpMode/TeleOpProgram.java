package org.firstinspires.ftc.teamcode.OpMode;

import com.qualcomm.robotcore.eventloop.opmode.OpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;

@TeleOp(name = "TeleOpProgram", group = "OpMode")
public class TeleOpProgram extends OpMode {

    @Override
    public void init() { //runs once when 'init' is pressed on the Driver Station

    }

    @Override
    public void loop() { //runs when the play button is pressed on the Driver Station and repeats/loops until the code is stopped

    }
}
